import React from 'react'

const AuthGroupLayout = (
    {
        children
    } : {
        children: React.ReactNode
    }
) => {
  return (
    <div>
        {children}
    </div>
  )
}

export default AuthGroupLayout